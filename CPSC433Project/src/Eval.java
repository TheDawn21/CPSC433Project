public class Eval {
    
    public static int calc(Schedule sched) {
        int score = Integer.MAX_VALUE;
        score = partialCalc(null);
        if(checkList(null) && isValid(null)) {

        }

        return 0;
    }
    
    public static boolean isValid(Schedule sched) {
        return true;
    }

    public static boolean checkList(Schedule sched) {
        return true;
    }

    public static int partialCalc(Schedule sched) {
        return 0;
    }
}
